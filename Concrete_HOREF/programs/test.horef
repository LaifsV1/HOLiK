import a0
import a1
import a2

public m1 x = {x + x} ;
public m2 x = {x * x} ;
private m3 x = {x - 4} ;
fun r1 := m1 ;
fun r2 := m2 ;
fun r3 := m3 ;
int r4 := 0 ;

r4 := 5 + 5 ;
let x = ((!r2) (!r4)) in
let y = ((!r1) (!r4)) in
let z = (!r3) (x + y) in
assert(1)