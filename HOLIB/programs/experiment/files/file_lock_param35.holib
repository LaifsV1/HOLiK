# set-bounds 3 5 #

int readMAX := 100;

public read (u:unit) :(unit) = {
  readMAX := !readMAX - 1;
  assert(0 < !readMAX)
};

// this example tests N=100 consecutive opponent questions
// trace should contain N consecutive calls and returns to read