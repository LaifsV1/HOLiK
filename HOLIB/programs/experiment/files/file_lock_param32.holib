# set-bounds 3 2 #

int readMAX := 4;

public read (u:unit) :(unit) = {
  readMAX := !readMAX - 1;
  assert(0 < !readMAX)
};

// this example tests N=4 consecutive opponent questions
// trace should contain N consecutive calls and returns to read