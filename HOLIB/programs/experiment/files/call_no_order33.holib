# set-bounds 3 3 #
import syscall :(int -> unit)

int a := 0;
int b := 0;
int c := 0;
int d := 0;

public isNone (i:int) :(unit) = {
  a := 1
};

public isA (i:int) :(unit) = {
  b := 1
};

public isAB (i:int) :(unit) = {
  c := 1
};

public isABC (i:int) :(unit) = {
  d := 1
};

public isABCD (i:int) :(unit) = {
  if !a && !b && !c && !d
  then assert(0)
  else ()
};

// K lists all 24 ways of causing failure, i.e. P(5,5)