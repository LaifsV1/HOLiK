# set-bounds 1 2 #
import send
import insert
import remove

public withdraw m = {
    if not (!funds < m)
    then send(m);
         funds := !funds - m;
	 assert(not (!funds < 0))
    else ()};
int funds := 100;